pipeline {
  agent any

  stages {
    
    stage('clone') {
      steps {
        git url: 'https://github.com/vvce23ise0094-hue/Jenkinsfile2.git',
          branch: 'main'
      }
    }

    stage('Run file2') {
      steps {
        py 'chmod +x file2.py'
        py './file2.py'
      }
    }
  }
}
