pipeline {
  agent any

  stages {
    
    stage('clone') {
      steps {
        git url: 'https://github.com/vvce23ise0094-hue/Jenkinsfile2.git',
          branch: 'main'
      }
    }

    stage('Run Python script') {
      steps {
        py 'chmod +x python.py'
        py './file2.py'
      }
    }
  }
}
